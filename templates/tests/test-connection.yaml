apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "novosga.fullname" . }}-test-connection"
  labels:
    {{- include "novosga.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "novosga.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
